import { useContext } from "react"
import { ForumContext } from "../main"

export const Question = () =>{
    //const {item} = useContext(ForumContext)
    return(
        <div>
            <h2>Question Page</h2>
        </div>
    )
}